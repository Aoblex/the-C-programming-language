/* Convert to lower case */
char lower(char);

/* Checks if a char is English letter */
int is_alphabet(char);

/* Checks if a char is a digit */
int is_digit(char);

/* Checks if a char is [0-9a-fA-f]*/
int is_hexchar(char);

/* Checks if a string is a valid hexadecimal number */
int valid_hexdigit(char[]);

/* Convert a hex char to decimal number*/
int char2hex(char);

/* Convert a hex string to decimal number*/
int htoi(char[]);