#include <stdio.h>
/* Deletes each character in s1 that
matches any character in s2. */
void squeeze(char s1[], char s2[]);

/* Reads a line no longer that lim
characters and returns its length. */
int get_line(char s[], int lim);